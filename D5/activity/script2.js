let startingNumber = parseInt(prompt("Enter Starting Number:"));
let endingNumber = parseInt(prompt("Enter Ending Number:"));

for (let i = startingNumber; i <= endingNumber; i++) {
    console.log("Number: ", i);
}
